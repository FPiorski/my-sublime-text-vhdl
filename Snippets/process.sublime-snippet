<snippet>
  <tabTrigger>PRO</tabTrigger>
  <scope>source.vhdl</scope>
  <content>PROCESS(i_clk)
BEGIN
    IF (rising_edge(i_clk)) THEN
        IF (i_rst_n = '0') THEN
            $0
        ELSE
        END IF;
    END IF;
END PROCESS;</content>
  <description>process</description>
</snippet>
